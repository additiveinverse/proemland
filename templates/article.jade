extends layout

block append vars
	- bodyclass = 'post'

block prepend title
	| #{ page.title + " - "}

block content
	article( class=bodyclass )
		h1( class=bodyclass + '-ttl ttl fn' )= page.title
		section(class=bodyclass + '-main')!= page.html
