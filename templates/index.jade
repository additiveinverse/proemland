extends layout

block append vars
	- bodyclass = 'home'

block content
	div( class='ps fld search-fld')
		label( for='searchInput', class='lbl search-lbl' )= Search
		input( ng-model='query', class='txt search-txt', id='searchInput', type='search', name='searchInput')

	ng-view

